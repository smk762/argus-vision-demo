# ── Frontend ──────────────────────────────────────────────────────────
# URL the browser uses to reach the argus-lens API server.
# Start the server with: argus-lens serve --cors --port 8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# Host port for the Next.js frontend (Docker only).
FRONTEND_PORT=3000
